<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skfkfkvlrm.couponservice.domain.coupon.CouponRepository">

	<!-- 만들어져있는 쿠폰 모두 조회 -->
	<select id="getCouponList" resultType="com.skfkfkvlrm.couponservice.domain.coupon.Coupon">
		SELECT coupon_id AS couponId, name, price, COALESCE(status, 'ON_SALE') AS status FROM coupons
	</select>

	<!-- 나의 가용포인트 조회 -->
	<select id="getStudentPoint" parameterType="String" resultType="java.lang.Integer">
		SELECT total_point FROM students WHERE student_id = #{studentId} LIMIT 1
	</select>

	<!-- 쿠폰 구매 (쿠폰구매내역 추가) -->
	<insert id="insertCouponPurchase">
		INSERT INTO coupon_purchase (price, name, state, created_date, student_id, coupon_id)
		VALUES(
			(SELECT price FROM coupons WHERE coupon_id = #{couponId}),
			(SELECT name FROM coupons WHERE coupon_id = #{couponId}),
			'사용전',
			NOW(),
			(SELECT id FROM students WHERE student_id = #{studentId} LIMIT 1),
			#{couponId}
		)
	</insert>

	<update id="updateCouponPurchaseStatus">
		UPDATE coupon_purchase
		SET state = CASE WHEN #{status} = 'USED' THEN '사용' ELSE #{status} END
		WHERE coupon_purchase_id = #{couponPurchaseId}
	</update>

	<!-- 나의 보유 쿠폰 조회 -->
	<select id="getMyCouponList" parameterType="String" resultType="com.skfkfkvlrm.couponservice.domain.coupon.CouponPurchase">
		SELECT cp.coupon_purchase_id AS couponPurchaseId, cp.name, cp.price, cp.state, cp.created_date as createdDate
		FROM coupon_purchase cp 
		INNER JOIN coupons c ON cp.coupon_id = c.coupon_id
		WHERE (cp.student_id = #{studentId} OR cp.student_id = (SELECT id FROM students WHERE student_id = #{studentId} LIMIT 1))
		ORDER BY cp.created_date DESC
	</select>

	<!-- 쿠폰 사용 처리 -->
	<update id="useCoupon">
		UPDATE coupon_purchase
		SET state = '사용'
		WHERE coupon_purchase_id = #{couponPurchaseId}
		  AND (student_id = #{studentId} OR student_id = (SELECT id FROM students WHERE student_id = #{studentId} LIMIT 1))
		  AND state = '사용전'
	</update>

	<!-- 내가 구매한 쿠폰 수량 -->
	<select id="getMyCouponCount" parameterType="String" resultType="int">
		SELECT COUNT(coupon_id) FROM coupon_purchase WHERE (student_id = #{studentId} OR student_id = (SELECT id FROM students WHERE student_id = #{studentId} LIMIT 1))
	</select>

	<!-- 관리자 쿠폰 CRUD -->
	<insert id="insertCoupon">
		INSERT INTO coupons (name, price, status)
		VALUES (#{name}, #{price}, COALESCE(#{status}, 'ON_SALE'))
	</insert>

	<update id="updateCoupon">
		UPDATE coupons
		SET name = #{name},
		    price = #{price},
		    status = COALESCE(#{status}, 'ON_SALE')
		WHERE coupon_id = #{couponId}
	</update>

	<delete id="deleteCoupon">
		DELETE FROM coupons WHERE coupon_id = #{couponId}
	</delete>

</mapper>
